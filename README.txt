0.  Вся разработка велась в docker вот этой сборки https://github.com/komissarovev/docker_php в приложении мой
    docker-compose.yml остальные файлы не менял

1.  Напишите функцию на PHP, которая принимает на вход матрицу вещественных чисел (единственный параметр) и возвращает
    её ранг. Пожалуйста не используйте библиотечные классы и методы для работы с матрицами. А также не используйте ц
    икл "for". (foreach и все остальное - можно)

1.1. В файле task1.php подключается файл с функциями functions.php, где расположена функция rand(). В файле task1.php
     есть примеры матриц разного размера для тестирования

2.🔸 Есть таблица Session. id, user_id, login_time, logout_time В ней хранится id пользователя, время входа и время
     выхода из системы (может быть null, если визит еще не завершен). Определить в какое время за отдельно взятые сутки
     в системе находилось одновременно максимальное число пользователей. Ответ: Скрипт на языке PHP, на вход принимает
     дату. Будут учитываться красота и оптимальность SQL запроса и алгоритма расчета, а также полнота выдаваемого
     скриптом ответа. *Здесь также будет важна скорость: для миллиона сессий скрипт должен отрабатывать не более чем
     за 5-10с.

2.1. Заходим в контейнер в Mysql
     $> docker exec -it tools_php-mysql bash

2.2. Подключаемся к СУБД
     $> mysql -uroot -p
     $> s123123
2.3. Создаем БД
     $> CREATE DATABASE `test_db`;

2.4. Создаем пользователя
     $> CREATE USER 'user1'@'%' IDENTIFIED BY 's123';

2.5. Даем ему права
     $> GRANT ALL PRIVILEGES ON `test_db` . * TO 'user1'@'%';

2.6. $> FLUSH PRIVILEGES;

2.7. Далее запускаем герератор http://localhost/generate_data.php для того, чтобы наполнить БД тестовыми данными,
     в $_GET нужно передать amount_events=100 чтобы задать количество генерируемых за раз элементов

2.8. Запускаем task2.php с ключом day в $_GET-параметре. day=1 - сегодня, $day=2 - вчера, $day=3 - позавчера и т.д.,
     чтобы выбрать нужный день, для которого будем искать период с максимальным количеством пользователей

     Конечно, решить данную задачу средствами SQL было бы элегантнее, но, поскольку так глубоко не владею SQL -
     попытался решить эту задачу на бэке.

     Алгоритм не оптимален, и не могу поручиться, что он посчитет на миллионе записей период за 5-10 сек...
     Но, если бы у меня было достаточно времени на эту задачу, я бы посмотрел в сторону того, чтобы из БД получать лог
     всех авторизованных пользователей чанками (скажем по 1000 записей) и обрабатывать параллельными процессами
     с увеличением счетчика в redis, например, для каждого интервала времени.

     Добавлю, если требуется решать быстро, я бы смотрел в сторону параллельной обработки (наверно PHP для этого не очень
     подходит). Если делать на PHP, то путем запука cron в несколько потоков, или в разных docker-контейнерах и
     огрегировать данные в данные через Redis();
